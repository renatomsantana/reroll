{
  "//": "Os testes de integração que abrem arquivo de verdade. Eles misturam código do renderer com `fs`, o que nenhum dos dois tsconfig aceita: o do renderer não tem os tipos do Node de propósito, e o do main não enxerga `src/renderer`.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node", "vite/client"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "baseUrl": ".",
    "paths": {
      "@renderer/*": ["src/renderer/src/*"],
      "@shared/*": ["src/shared/*"]
    }
  },
  "//include": "Os `.d.ts` do renderer entram junto: um teste de integração que importa componente de lá precisa da declaração global de `window.api`, que mora em `types/window.d.ts`. Sem ela, o import compila em todo lugar menos aqui.",
  "include": [
    "src/**/*.node.test.ts",
    "src/**/testes/**/*.ts",
    "src/renderer/src/types/**/*.d.ts"
  ]
}
